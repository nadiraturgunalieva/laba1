/* Упражнение 02. Динамическая память в С++. Указатели
* 
* С - С++:
1) int *ptr = (int*)malloc(sizeof(int)) - int*
2) int *ptrArr = (int*)malloc(sizeof(int)*N) - …
3) free(ptr) - …
4) free(ptrArr) - …
В С++ необходимо ли освобождать динамическую память? Можно ли освободить одну и ту же
выделенную1 память дважды? А можно ли освободить невыделенную память?
* 
* 
*/